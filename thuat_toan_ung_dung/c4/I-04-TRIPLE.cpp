#include <bits/stdc++.h>
#include <iostream>

int n, k;

int result = 1000000000;
int temp_cost = 0;
int src;
bool check;

int main()
{
    std::ios::sync_with_stdio(false);
    std::cin >> n;
    std::cin >> k;

    int list_num[n];
    for(int i = 0; i < n; i++)
    {
        std::cin >> list_num[i];
    }

    std::sort (list_num,list_num+n);
    int sum = 0;

    do {
        sum += 

    } while ( std::next_permutation(list_num, list_num+n) );
}